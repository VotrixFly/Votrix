<UserControl x:Class="Votrix.Pages.ServerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Votrix.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:s="https://github.com/canton7/Stylet"
             d:DataContext="{d:DesignInstance local:ServerListViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="65"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!--左上角名称与按钮-->
        <StackPanel Background="{DynamicResource PrimaryHueMidBrush}" Orientation="Horizontal">
            <TextBlock Text="服务列表" Height="32" Foreground="{DynamicResource PrimaryHueLightForegroundBrush}" Padding="15,0,0,0" FontSize="18" VerticalAlignment="Bottom" Margin="0,0,0,-0.2" Width="175"/>
            <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{s:Action AddServer}" Width="24" Height="24" Padding="0" VerticalAlignment="Bottom" Margin="0,0,0,8" Background="{DynamicResource PrimaryHueLightBrush}">
                <materialDesign:PackIcon Kind="Add" Height="24" Width="24" Foreground="White"/>
            </Button>
        </StackPanel>

        <!--左侧服务器列表-->
        <ListBox Grid.Row="1" Grid.Column="0" Background="{DynamicResource PrimaryHueMidBrush}" ItemsSource="{Binding ServerList}">
            <ListBox.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource MaterialDesignScrollBarMinimal}"/>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="domain:DemoItem">
                    <Grid Height="50">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Grid.RowSpan="2" Grid.Column="0" Style="{StaticResource MaterialDesignFlatButton}" Padding="0">
                            <materialDesign:PackIcon Kind="AlphabetVBoxOutline" Height="30" Width="30" Foreground="White"/>
                        </Button>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="Myv2ray" Foreground="{DynamicResource PrimaryHueLightForegroundBrush}" FontSize="15" VerticalAlignment="Bottom"/>
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" >
                            <TextBlock  Text="127.0.0.1" Foreground="#FFA89C9C" FontSize="15" VerticalAlignment="Bottom"/>
                            <TextBlock  Text=":" Foreground="#FFA89C9C" FontSize="15" VerticalAlignment="Bottom"/>
                            <TextBlock  Text="1080" Foreground="#FFA89C9C" FontSize="15" VerticalAlignment="Bottom"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--右侧设置-->
        <Grid Grid.RowSpan="2" Grid.Column="1" Background="{DynamicResource PrimaryHueLightBrush}">
        </Grid>

    </Grid>
</UserControl>
